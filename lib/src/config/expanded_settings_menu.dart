import 'package:flutter/material.dart';

class ExpandedSettingsMenu extends StatelessWidget {
  const ExpandedSettingsMenu({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Column(
        // Code Here
        );
  }
}
